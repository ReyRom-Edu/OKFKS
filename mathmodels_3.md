# Математические модели ошибок в программах

**Материалы:** книга `Практикум.xlsx`. Все данные учебные. Формулы уже введены; студент может повторять их в своей копии. Желтые ячейки — данные, которые можно менять. «Поиск решения», макросы и дополнительные надстройки не нужны.

## Задача компании и необходимые понятия

Компания «СофтСервис» готовит сервис обработки заказов. Руководитель спрашивает:

1. Как часто тесты заканчиваются ошибкой?
2. Какова вероятность, что программа проработает час без отказа?
3. Сколько новых дефектов найдет команда, если продолжит тестирование?

Для этих вопросов нужны разные данные. В рамках одной компании рассмотрим **три отдельных опыта**. Нельзя объединять их числа: число неуспешных тестов, число отказов и число уникальных дефектов — разные показатели.

| Понятие | Простое объяснение | Пример |
|---|---|---|
| Дефект | Ошибка внутри программы | Неправильная формула скидки |
| Отказ | Программа нарушила требуемое поведение | Покупателю рассчитали неверную сумму |
| Неуспешный тест | Результат проверки не совпал с ожидаемым | Автотест показал неправильную сумму |

Пять тестов обнаружили одну неправильную формулу скидки. Сколько дефектов? **Один**, хотя неуспешных тестов пять.

Математическая модель — упрощенное описание ситуации с помощью формул. Она помогает сделать прогноз, но работает при определенных условиях.

На занятии используем четыре обозначения:

- $p$ — вероятность неуспешного теста, от 0 до 1;
- $\lambda$ («лямбда») — среднее число отказов за час;
- $R$ — вероятность работы без отказа, от 0 до 1;
- $m(t)$ — сколько дефектов модель ожидает обнаружить к моменту $t$.

**Среднее ожидаемое число** — ориентир для многих похожих опытов. Значение 1,2 ошибки не означает существование «одной целой и двух десятых ошибки»: в отдельном опыте получится целое число, например 0, 1 или 3.

## Опыт № 1. Сколько тестов окажется неуспешными? Модель Бернулли

### Данные и применимость

Команда выполняет 1000 тестов одной сборки. Неуспешных тестов — 12. Во время этого опыта программу не исправляют.

**Модель Бернулли** описывает один тест с двумя исходами, а **биномиальная модель** — количество неуспехов в серии таких тестов.

$$P(X=k)=C^k_n \cdot p^k \cdot (1-p)^{n-k}$$

$$M(X) = np$$
$$D(X) = npq$$

Применяем ее, если тесты независимы и вероятность неуспеха примерно одинакова. Для прогноза будущие тесты должны быть сопоставимы с выполненными. Нельзя переносить результат только простых тестов на сложные сценарии.

### Решение

Сначала оценим вероятность неуспешного теста:

$$p=\frac{\text{неуспешных тестов}}{\text{всех тестов}}=\frac{12}{1000}=0{,}012=1{,}2\%.$$

Пусть предстоит выполнить еще $n=100$ тестов. Ожидаемое число неуспехов:

$$\text{Ожидаемые неуспехи}=np=100\cdot0{,}012=1{,}2.$$

Вероятность успеха одного теста равна $1-p$. Если тесты независимы, вероятность успеха всех тестов перемножается:

$$P(\text{все успешны})=(1-p)^n=(1-0{,}012)^{100}\approx29{,}9\%.$$

Тогда вероятность хотя бы одного неуспеха:

$$P(\text{хотя бы один неуспех})=1-(1-p)^n\approx70{,}1\%.$$

**Что сказать руководителю:** доля неуспехов невелика, но в большой серии вероятность встретить хотя бы один неуспешный тест уже значительна.

---

### Повторяем в Excel: лист «Тесты»

Входные данные уже находятся в желтых ячейках:

| Ячейка | Значение | Смысл |
|---|---:|---|
| B5 | 1000 | Выполнено тестов |
| B6 | 12 | Неуспешных тестов |
| B7 | 100 | Будущих тестов |

Последовательно введите или разберите формулы:

| Ячейка | Формула | Ответ |
|---|---|---:|
| B10 | `=B6/B5` | 1,2% |
| B11 | `=B7*B10` | 1,2 |
| B12 | `=(1-B10)^B7` | 29,9% |
| B13 | `=1-B12` | 70,1% |

Для B10, B12 и B13 нужен процентный формат ячеек. Знак `^` означает возведение в степень.

**Мини-опыт:** измените B7 со 100 на 200. Ожидаемое число неуспехов станет 2,4. Вероятность хотя бы одного неуспеха вырастет примерно до 91,1%. После обсуждения верните 100.

**Граница применимости:** модель не показывает, сколько уникальных дефектов осталось в программе.

## Опыт № 2. Сколько будет отказов и проработает ли программа час? Модель Пуассона

### Данные и применимость

В отдельном опыте фиксированная сборка работала 20 часов. Зарегистрировано 5 отказов. Работу после отказа восстанавливали, но код во время опыта не меняли. Нагрузка оставалась примерно одинаковой.

**Модель Пуассона** описывает число отказов за период. Связанная с ней **экспоненциальная модель** описывает ожидание следующего отказа и вероятность работы без отказа.

$$P(X=k)= \frac{\lambda^k \cdot e^{−\lambda}}{k!}$$

$$M(X)=D(X)=\lambda$$

Параметр $\lambda$ (лямбда) — это среднее число событий за рассматриваемый интервал.

Обе модели в нашем примере предполагают примерно постоянную интенсивность и отсутствие зависимых серий отказов.

### Шаг 1. Интенсивность

$$\lambda=\frac{\text{число отказов}}{\text{время наблюдения}}=\frac5{20}=0{,}25\text{ отказа/ч}.$$

Это означает в среднем один отказ на четыре часа наблюдений. Отказы не обязаны происходить через равные интервалы.

### Шаг 2. Ожидаемое число отказов

Для будущего периода длительностью $t$:

$$\text{Ожидаемое число отказов}=\lambda t.$$

За один час: $0{,}25\cdot1=0{,}25$. За четыре часа: $0{,}25\cdot4=1$.

Для базового рассчета не нужно вычислять вероятности всех возможных количеств отказов. Достаточно понять, что модель Пуассона допускает 0, 1, 2 и больше отказов, а $\lambda t$ задает среднее.

### Шаг 3. Вероятность работы без отказа

$$R(t)=e^{-\lambda t}.$$

Здесь $e\approx2{,}718$ — математическая постоянная.

За один час:

$$R(1)=e^{-0{,}25}\approx77{,}9\%.$$

За четыре часа:

$$R(4)=e^{-1}\approx36{,}8\%.$$

**Вопрос группе:** если среднее время между отказами равно четырем часам, гарантированы ли четыре часа работы? **Нет.** По этой модели вероятность пройти четыре часа без отказа — лишь около 36,8%.

### Повторяем в Excel: лист «Отказы»

| Ячейка | Значение | Смысл |
|---|---:|---|
| B5 | 20 | Часы наблюдения |
| B6 | 5 | Число отказов |
| B7 | 1 | Будущий период, ч |

| Ячейка | Формула | Ответ при B7 = 1 |
|---|---|---:|
| B10 | `=B6/B5` | 0,25 отказа/ч |
| B11 | `=B10*B7` | 0,25 отказа |
| B12 | `=1/B10` | 4 ч |
| B13 | `=EXP(-B10*B7)` | 77,9% |
| B14 | `=1-B13` | 22,1% |

В готовой книге формула B12 дополнительно обрабатывает нулевую интенсивность, чтобы не возникало деления на ноль. Для наших данных она дает тот же ответ.

**Мини-опыт:** измените B7 с 1 на 4. Получатся ответы, рассчитанные выше. Затем верните 1.

**Что сказать руководителю:** по данным этого опыта час работы без отказа оценивается примерно в 78%. Это оценка при стабильной нагрузке, а не гарантия.

**Граница применимости:** если программу активно исправляют и интенсивность снижается, одной постоянной $\lambda$ уже недостаточно. Если отказов не наблюдали, это тоже не доказывает абсолютную надежность: наблюдение могло быть слишком коротким.

## 4. Опыт № 3. Как исправления меняют число найденных дефектов? Модель Гоэла—Окумото

### Исходный журнал: лист «Исправления»

Теперь команда тестирует программу, находит и исправляет дефекты. Повторные сообщения об одном дефекте не считаются новыми. Время — часы активного тестирования; нагрузка и сложность тестов сопоставимы.

| Конец периода, ч | Новых дефектов за 10 ч | Всего найдено |
|---:|---:|---:|
| 10 | 18 | 18 |
| 20 | 15 | 33 |
| 30 | 12 | 45 |
| 40 | 10 | 55 |
| 50 | 8 | 63 |
| 60 | 7 | 70 |
| 70 | 5 | 75 |
| 80 | 4 | 79 |
| 90 | 3 | 82 |
| 100 | 2 | 84 |

**Начните с наблюдения:** новых дефектов за период становится меньше, но суммарное число найденных продолжает увеличиваться. Это два разных графика.

### Шаг 1. Обрабатываем журнал

В книге столбец A содержит конец периода, B — новые дефекты.

В C6:

```excel
=SUM(B$6:B6)
```

Протяните до C15. Получите накопленное число найденных дефектов. Знак `$` закрепляет строку 6: начало суммирования не сдвигается при копировании.

Частота обнаружения в первом периоде, E6:

```excel
=B6/A6
```

В E7 и далее:

```excel
=B7/(A7-A6)
```

Она снизилась с 1,8 до 0,2 нового дефекта в час. Это частота **обнаружения уникальных дефектов при исправлениях**, а не автоматически частота отказов работающего продукта.

### Шаг 2. Выбираем модель

Используем модель **Гоэла—Окумото**:

$$m(t)=a(1-e^{-bt}).$$

Смысл величин:

- $t$ — сколько часов уже тестировали;
- $m(t)$ — сколько дефектов ожидается найти к этому времени;
- $a$ — уровень, к которому приближается кривая накопленных обнаружений;
- $b$ — параметр скорости: насколько быстро кривая приближается к этому уровню.

Производные и теория случайных процессов для этого занятия не нужны. Достаточно трех свойств:

1. В начале $m(0)=0$.
2. С увеличением времени найденных дефектов становится больше.
3. Прирост постепенно уменьшается, а кривая приближается к $a$.

Модель разумна, когда дефекты исправляются успешно, новые дефекты при исправлениях не появляются, а условия тестирования не меняются резко.

### Шаг 3. Получаем параметры

Разделим 100 часов на **две равные части по 50 часов**:

- в первой половине найдено $u=63$ новых дефекта;
- во второй половине найдено $v=21$ новый дефект.

Во второй половине обнаружений в три раза меньше:

$$q=\frac vu=\frac{21}{63}=\frac13.$$

Для выбранной модели при равных периодах длительностью $T$ можно использовать простую оценку:

$$q=e^{-bT}$$

$$a=\frac{u}{1-q},\qquad b=\frac{-\ln q}{T}.$$

Получаем:

$$a=\frac{63}{1-1/3}=94{,}5;\qquad b=\frac{-\ln(1/3)}{50}\approx0{,}02197.$$

**Почему это работает:** в модели первое приращение равно $a(1-q)$, второе — $aq(1-q)$, где $q=e^{-bT}$. Их отношение равно $q$. Подробный вывод можно пропустить.

Это учебный способ оценки по двум суммам, а не статистическая оптимизация по всем десяти интервалам. Он применим здесь при $u>0$ и $0<v<u$. Если во второй половине находок столько же или больше, либо их совсем нет, эта простая конечная оценка не подходит.

Разбор ячеек:

| Ячейка | Смысл | Основная формула | Ответ |
|---|---|---|---:|
| H5 | Длина половины | `=A10` | 50 |
| H6 | Первая половина | `=SUM(B6:B10)` | 63 |
| H7 | Вторая половина | `=SUM(B11:B15)` | 21 |
| H8 | Отношение q | `=H7/H6` | 0,3333 |
| H9 | Параметр a | `=H6/(1-H8)` | 94,5 |
| H10 | Параметр b | `=-LN(H8)/H5` | 0,02197 |

В готовой книге в H8:H10 добавлена проверка условий применимости. 

### Шаг 4. Строим кривую и прогноз

В D6 введите основную формулу модели:

```excel
=$H$9*(1-EXP(-$H$10*A6))
```

Протяните до D15. Ссылки на параметры закреплены знаком `$`, а время A6 меняется по строкам. Готовая книга дополнительно проверяет наличие корректных параметров.

Сравните столбец C «Всего найдено» со столбцом D «Модель: всего». В книге уже есть диаграмма. Для самостоятельного построения выделите A5:A15 и C5:D15 и вставьте график с двумя рядами данных; A задает время. При неравных шагах времени лучше использовать точечную диаграмму с линиями.

![Наблюдения и простая модель роста числа обнаруженных дефектов](.\Resources\Рост_надежности_пример.png)

Теперь оценим, сколько новых дефектов будет найдено за дополнительные 20 часов:

$$\text{Прогноз новых}=m(120)-m(100).$$

По модели:

$$m(100)=84;\qquad m(120)\approx87{,}73.$$

Следовательно:

$$m(120)-m(100)\approx3{,}73.$$

В Excel:

| Ячейка | Действие | Основная формула |
|---|---|---|
| H12 | Введите дополнительные часы | `20` |
| H13 | Всего к концу будущего периода | `=H9*(1-EXP(-H10*(A15+H12)))` |
| H14 | Новые за будущий период | `=H13-D15` |
| H15 | Модельный остаток на 100 ч | `=H9-D15` |

Ответы: H13 — **87,73**, H14 — **3,73**, H15 — **10,5**.

**Что сказать руководителю:** при продолжении тестирования в похожих условиях модель ожидает примерно четыре новых дефекта за следующие 20 часов. Это ориентир для планирования, не обещание точного количества.

Параметр $a=94{,}5$ и остаток 10,5 не являются точным подсчетом ошибок в коде. Дефекты, не обнаруживаемые выбранными тестами, могут не отражаться в таком прогнозе.

**Важное ограничение:** модель построена по уже известным данным. Совпадение графика с прошлым еще не проверяет прогноз. После следующих 20 часов компания должна сравнить новые наблюдения с предсказанием 3,73.

## Как выбрать подходящую модель

| Задача компании | Какие данные есть | Модель | Когда применять |
|---|---|---|---|
| Оценить неуспехи в серии тестов | Число тестов и неуспехов | Биномиальная | Одна сборка, независимые сопоставимые тесты |
| Оценить число отказов за период | Время работы и число отказов | Пуассона | Примерно постоянная интенсивность |
| Оценить вероятность работы без отказа | Интенсивность и нужный период | Экспоненциальная | Те же условия постоянной интенсивности |
| Оценить новые находки при продолжении исправлений | Журнал новых дефектов по времени | Гоэла—Окумото | Частота обнаружений снижается при стабильном процессе тестирования |

### Три задания

1. На листе «Тесты» замените 12 неуспехов на 6. Как изменится вероятность ошибки? Сколько неуспехов ожидается в 100 тестах?
2. На листе «Отказы» задайте будущий период 2 часа. Какова вероятность пройти его без отказа?
3. На листе «Исправления» задайте дополнительно 50 часов. Сколько новых дефектов ожидается?

**Ответы:**

1. $p=0{,}6\%$, ожидаемое число — 0,6.
2. $R(2)=e^{-0{,}5}\approx60{,}7\%$.
3. $m(150)-m(100)=91-84=7$ новых дефектов.

После упражнений верните исходные значения: 12 неуспехов, 1 час, 20 дополнительных часов.

**Задание на понимание:** новых дефектов стало вдвое меньше, но команда выполняет вдвое меньше тестов. Можно ли утверждать, что качество улучшилось? Нет: сначала нужно учесть изменение объема и состава тестирования.

## 6. Закрепление

1. Почему число неуспешных тестов не равно числу дефектов?
2. Чем вероятность отличается от среднего количества?
3. Почему прогноз по времени требует сопоставимой нагрузки?
4. Почему исправления делают модель постоянной интенсивности менее подходящей?
5. Что означает прогноз «3,73 новых дефекта»?

Ожидаемые ответы: один дефект может провалить несколько тестов; вероятность лежит между 0 и 1, количество может быть больше 1; нагрузка влияет на число отказов; исправления меняют интенсивность; 3,73 — среднее ожидаемое количество, а не точный результат.

**Практический вывод:** сначала определяют, что считается и меняется ли программа. Затем выбирают модель, выполняют расчет и проверяют прогноз новыми наблюдениями. Одного числа недостаточно, чтобы объявить программу готовой к выпуску: нужно также учитывать критичность дефектов и проверенные сценарии.

### Источники

- [NIST: биномиальное распределение](https://itl.nist.gov/div898/handbook/eda/section3/eda366i.htm).
- [NIST: экспоненциальное распределение и связь с процессом Пуассона](https://www.itl.nist.gov/div898/handbook/apr/section1/apr161.htm).
- [Исследование моделей роста надежности и влияния тестовых усилий](https://www.ijpe-online.com/EN/10.23940/ijpe.19.05.p1.12651272).
